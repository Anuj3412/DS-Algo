  // if(pos == 1 or pos == 0){
    //     //create a new node and make it head of the list.
    //     node* newNode = new node(data );
    //     newNode->next = head;
    //     head = newNode;
    // }